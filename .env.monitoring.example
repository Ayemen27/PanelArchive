# ==============================================================================
# Monitoring Environment Variables - Prometheus & Grafana
# ==============================================================================
# نسخ هذا الملف إلى .env وقم بتعيين القيم الآمنة
# cp .env.monitoring.example .env (ثم أضف القيم للـ .env الموجود)
# ==============================================================================

# ==============================================================================
# Grafana Configuration
# ==============================================================================

# ⚠️ Admin Credentials (يجب تغييرها!)
# لا تستخدم admin/admin في الإنتاج أبداً
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=ChangeThisSecurePassword123!

# Server Domain (غيّره للدومين الخاص بك)
GRAFANA_DOMAIN=localhost

# ==============================================================================
# ملاحظات مهمة - Important Notes
# ==============================================================================
#
# 1. الأمان - Security:
#    ⚠️ يجب تغيير GRAFANA_ADMIN_PASSWORD قبل النشر للإنتاج
#    ⚠️ استخدم كلمة مرور قوية (12+ حرف، أرقام، رموز)
#    ⚠️ في الإنتاج: أزل ports من docker-compose.yml واستخدم nginx
#
# 2. في الإنتاج:
#    - أزل port mappings (9090:9090 و 3000:3000)
#    - استخدم nginx reverse proxy مع SSL/TLS
#    - فعّل basic authentication على Prometheus
#    - قيّد الوصول للـ IP addresses المحددة
#
# 3. للتطوير المحلي:
#    - يمكن استخدام admin/admin مؤقتاً
#    - تأكد من عدم تعريض المنافذ خارج localhost
#
# ==============================================================================
